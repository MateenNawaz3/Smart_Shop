//
//  HandoffView.swift
//  SmartShop
//

// STRUCTURE ONLY — not implemented yet.
//
// Identify me at a terminal — NEW SCREEN.
//
// Shows the single-use token from POST /mobile/handoff/token as a QR code, and
// emits it over NFC where the handset supports it.
//
// The token is valid for 60 seconds, so the screen needs a visible countdown and
// a re-request when it lapses. Most naturally reached from the home screen or
// the tab bar.
